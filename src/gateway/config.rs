// SPDX-License-Identifier: AGPL-3.0-or-later

// src/gateway/config.rs
//
// Configuration has moved to `crate::config`.
// This file is kept only so that any external crate that imports
// `mira::gateway::config::*` still compiles.

pub use crate::config::*;
